{
  "name": "devops-thessaloniki-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "predev": "node scripts/copy-event-assets.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/copy-event-assets.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "check:links": "node scripts/check-event-asset-links.mjs",
    "check": "npm run build && npm run check:links",
    "gen:og": "node scripts/generate-og-image.mjs"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
