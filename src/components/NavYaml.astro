---
interface Props {
  yamlNav: string;
}
const { yamlNav } = Astro.props;
---
<pre class="text-sm lg:text-base leading-relaxed whitespace-pre-wrap break-words" set:html={yamlNav} />
<div class="mt-3 text-sm text-cream/50">
  <span class="text-accent">$</span> <span class="caret">▊</span>
</div>
